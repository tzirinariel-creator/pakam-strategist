# פכמון (pakam-strategist) — קרא את זה לפני כל פעולה

אפליקציית תכנון־תואר לסטודנטים של פכ"מ בת"א. **מוצר אמיתי בפרודקשן עם ~24 משתמשים אמיתיים** (pakam-strategist.vercel.app), לא ניסוי. הבעלים: אריאל.

## סדר קריאה חובה בתחילת סשן (לפי הסדר)
1. `docs/חוקת-הפרויקט.md` — מי הבעלים, העקרונות, מה מטריד אותו, איך מדווחים לו. **בלי זה תיכשל גם אם הקוד שלך מושלם.**
2. `docs/תוכנית-ביצוע-סופית.md` — ההחלטות, שלבי הביצוע, קריטריוני־קבלה, החלטות־בעלים.
3. `docs/מפרטי-ביצוע-לפועלים.md` — **גובר על התוכנית בשלבים P/E/S**: קואורדינטות בקוד, 10 כללי־התנהגות, עצי־אבחון.
4. **`docs/רישום-הערות-ראשי.md` — מקור האמת היחיד לסטטוס של כל הערה של אריאל.**
   הערה חדשה נכנסת לשם **מיד, בטקסט של אריאל, לפני שנוגעים בקוד**. ✅ רק עם
   הוכחה של מה שנראה על מסך אמיתי — "הקוד עושה X" אינו הערה סגורה.
5. `docs/משימות-בקרה.md` — יומן הסבבים (ארכיון). מתעדכן בכל סוף פרוסה.

## חוקי ברזל (עוקף כל שיקול אחר)
- `git` ממופה ל־`true` — תמיד `command git`, ואמת HEAD מול origin אחרי push.
- מסד־נתונים אחד = פרודקשן (Supabase). מיגרציות: תוספות בלבד, `npx prisma migrate deploy --config prisma/prisma.config.ts`, לפני קוד שתלוי בהן.
- חשבון הדמו קריאה־בלבד. בדיקות־כתיבה רק ב־vitest.
- פריסה: **`npx -y vercel@48 --prod --yes`**. ה-CLI הלא-מוצמד (`npx vercel`) נשבר על `@vercel/fastify@6.0.0` שלא קיים ברג׳יסטרי, ו-`vercel@46` נכשל על טוקן. על "Not authorized" — נסה שוב פעם אחת. אמת Ready + עמוד 200.
- שער איכות לפני כל commit: **`npm run lint` (0 errors)** + `npx tsc --noEmit` + `npx vitest run` ירוקים + אימות דפדפן + סקירת־דיף עצמית.
  ה־CI ב־GitHub מריץ **lint, tsc, test** — שער שמדלג על lint הוא שער שנכשל ב־CI בלי שנדע. אחרי push: `gh run list --limit 1`.
- **לעולם לא**: לנבא נקודות בידינג / להמציא תאריך או ציון או נתון / להציג נתון בלי מקור.
- אנגלית לא בממוצע התואר; `englishLevel` מוצהר גובר על אמירם (דרך `resolveEnglishLevel`).
- RTL: לעולם לא `dir="ltr"` על טקסט עם מילה עברית — רק `<bdi dir="ltr">` על המספר/תאריך.
- Tailwind v4: אסור `@layer components`; אסור `*/` בהערת CSS; popover/modal תחת אב עם transform → פורטל.
- מונחים: ש״ס (בגרשיים), לא נ"ז; "תחום מיקוד". קופי מוצר ברבים; צ'אט היועץ ביחיד ממוגדר.
- אייקוני AI גנריים (Sparkles וכו') אסורים — רק סמלי המותג (המלך/הרפרנט).
- צילום־מסך של הכלי לא אמין לפריסה — מדוד DOM (`getBoundingClientRect`, הגלאי בתוכנית §0.3).
- החלטות־בעלים (תוכנית §9) — לעולם לא להחליט לבד. כשהערה של אריאל סותרת החלטה מתועדת בקוד — עצור ושאל.
- **שער סגירת ההערות (תוכנית §16):** לפני הכרזת סיום — סגירה הערה־הערה מול `docs/הערות-המקור-של-אריאל.md` (הטקסט המקורי!), כולל כל תתי־החלקים, אל תוך `docs/סגירת-51-הערות.md`. הערה גבולית לא מאשרים לבד — שואלים את אריאל.

## עדכון ידע
בסוף כל פרוסה: עדכן `docs/משימות-בקרה.md`. תובנה חדשה ששייכת לחוקה/תוכנית — הוסף שם, לא רק בצ'אט. **ידע שנשאר רק בשיחה = ידע שיימחק.**

## שער האיכות — `npm run gate` (ולא לקרוא שורות)
**3.9: ה-CI נפל פעמיים על main בגלל טעות קריאה שלי, לא בגלל הקוד.** הרצתי
`npm run lint | tail -2` וקראתי את `"0 errors and 2 warnings potentially
fixable"` כאילו זה מספר השגיאות. זו שורת ה־**auto-fixable**, ו-
`rules-of-hooks` ו-`Cannot access refs during render` אינן כאלה. השורה
האמיתית היא `✖ N problems (X errors, Y warnings)` ו-`tail -2` חתך אותה.

**לכן: `npm run gate`** — מריץ בדיוק את שלושת הצעדים של ה-CI (`npm run lint`
שהוא `eslint .`, `npx tsc --noEmit`, `npx vitest run`) ובודק **קודי יציאה**.
לעולם לא להסיק "0 שגיאות" מפלט חתוך. אחרי push: `gh run list --limit 1`.

## מלכודת הכלים — `waitForFunction` והמתנות שנכשלות בשקט
**5.9: 39 המתנות בכל כלי הבדיקה קיבלו 30 שניות במקום מה שכתבתי.**
החתימה היא `waitForFunction(fn, arg, options)`. כתבתי
`waitForFunction(() => …, { timeout: 150000 })` — והאובייקט נבלע
כ־**ארגומנט לפונקציה**, כך שחלה ברירת המחדל. רובן עטופות
ב־`.catch(() => {})`, ולכן נכשלו **בשקט** והסקריפט המשיך לקרוא עמוד
שעוד לא נטען.

זה ייצר ערב שלם של "באגים" מדומים: תשע הערות "לא מיושמות", עמודים
"ריקים", וזריעת פרסונה שנפלה והשאירה חשבון ריק מתויג "שנה ב׳".
**תמיד `waitForFunction(fn, null, { timeout })`.**

### שלושת השערים שנולדו מזה
1. **התחברות באתחול, לא בתוך בדיקה.** ב-`verify-all.mjs` היא ישבה בתוך
   M31; כל ריצה שדילגה על M31 בדקה מסכים מוגנים בלי להיות מחוברת.
2. **שער פרסונה** (`final-personas.mjs`) — קורא את השנה מהלוח לפני
   הסיור ויוצא אם היא לא תואמת. **סיור מתויג לא נכון גרוע מסיור שלא רץ**,
   כי הוא נראה כמו ראיה.
3. **גלאי גלישה מודע ל־scroller** — אלמנט שחורג מהמסך בתוך מיכל
   `overflow-x` אינו באג אלא הדרך הנכונה. בלי זה דווחו 12 ממצאי שווא
   (מערכת השעות, ציר הזמן, טבלת הקטלוג — `ui/table.tsx:11`).

## בריכת החיבורים — נפתר ב-5.9, אל תחזיר אחורה
`DATABASE_URL` הוא **transaction mode** — פורט **6543** עם
`?pgbouncer=true`. החיבור חוזר לבריכה אחרי כל טרנזקציה, כך שעשרות מכונות
Vercel חולקות מעט חיבורי־שרת.

**איך הגענו לזה:** בפורט 5432 (session mode) כל חיבור נתפס לכל חיי
המכונה, וכל הפרויקט חלק `pool_size: 15`. עם `max: 5` ב-`src/lib/db.ts`,
**שלוש מכונות חמות הפילו את פרודקשן** (`EMAXCONNSESSION`) בלילה 4→5.9.
פריסה מחדש מיחזרה את המכונות והחזירה את האתר.

**אומת אחרי המעבר:** שמונה מסכים עם נתונים אמיתיים · כתיבה ששרדה רענון ·
**30 בקשות בו-זמנית, אפס כשלים** · אפס שגיאות JS.

`max: 2` נשאר ב-`db.ts` — הוא כבר לא הכרחי ב-transaction mode, אבל הוא
שמרני ועובד. **אל תעלה אותו בלי מדידה.**
גלגול לאחור, אם אי-פעם יידרש: פורט 5432 בלי מחרוזת השאילתה.
כלי הבדיקה: `npx tsx scripts/probe-transaction-mode.ts`.

## תו בלתי־נראה בסוף משתנה־סביבה — 6.9
**החיבור ליומן Google לא עבד מעולם בפרודקשן, ולא בגלל הקוד.** שמונה
משתני־סביבה שם נשמרו עם **תו שורה חדשה בסוף הערך**. אחד מהם הוא
`GOOGLE_REDIRECT_URI`, וגוגל דוחה כל בקשה בגללו. פענוח מטען־השגיאה של גוגל
נקב בזה מפורשות: `redirect_uri = ".../api/google/callback\n" →
invalid_request`. לוג השרת לא הראה כלום — הכישלון קרה אצל גוגל.

**לכן: ערך שמגיע מ-`process.env` הוא קלט, ומחתכים אותו.** `src/lib/env.ts`
(`env()` / `envOr()`) הוא הצינור. לבדוק חשד כזה:
`npx vercel@48 env pull … && grep '\\n"$' .env.prod`.

## המרחק לסידני — 1.15 שניות לכל בקשה
פונקציות ה-API רצות ב-`fra1` (`vercel.json`) והמסד ואימות המשתמשים
ב-**ap-southeast-2**. כל בקשה עושה 3–4 הלוך־חזור של ~280ms. נמדד בפרודקשן
6.9: `schedule.getGoogleStatus` מחזיר **68 בתים ב-1158ms**; באטץ׳ מסך הבית
2956ms. זו רצפה של מרחק, לא של קוד — הגדלת בריכת החיבורים מ-2 ל-6 לא הזיזה
אותה. המנוף האמיתי: `preferredRegion = "syd1"` על `/api/trpc`. לא בוצע —
Vercel מגן על preview ב-302 ואי־אפשר למדוד בלי לגעת בפרודקשן.

## הידיעון כמקור־אימות — שתי הלשוניות
ייצוא DOCX של עמוד הידיעון מכיל את **לשונית "תוכנית לימודים"** (קוד, שם,
פירוק שעות, סה״כ ש״ס, שנה, וסל חובה/בחירה) ואת **לשונית "מערכת שעות"**
(קבוצה, אופן הוראה, סמסטר, מרצה, מיקום, יום ושעה). זה המקור היחיד שאפשר
לאמת מולו ש״ס ושעות.

`scripts/audit-yedion-5787-program.ts` ו-`scripts/audit-yedion-schedule.ts`
עושים את ההשוואה. **שתיהן חייבות להשוות בשני הכיוונים:** השוואה בכיוון אחד
בלבד מחפשת התאמה לשורת־ידיעון, ושורה **מיותרת אצלנו** — נתון של שנה קודמת
שנשאר — פשוט לא נבדקת. כך נמצא שהשיעור של "פוליטיקה השוואתית" ישב אצלנו
בסמסטר א׳ ביום ראשון בזמן שהידיעון אומר סמסטר ב׳ ביום שלישי.

מלכודות פרסור שכבר נפלתי בהן: קוד יכול להיות תלת־חלקי (`1411-9101-10`);
בין הקוד לשם יכולים לשבת שורה ריקה וסימן־הערה `(3)`; שורת־קוד שאחריה
שורת־קוד היא שארית ולא קורס; ולקבוצה אחת יכולים להיות **כמה מפגשים**
(שיעור שנפגש פעמיים בשבוע) — פרסור שלא נושא את כותרת הקבוצה קדימה מייצר
עשרות ממצאי־שווא.

## הצטיינות נמדדת על שנה שהסתיימה
`computeHonorsDistance` מקבל שנה, ומדידה של **השנה הנוכחית** מחזירה ריק
לכל סטודנט שהשנה שלו עוד לא התחילה — כלומר רוב הזמן. משתמשים תמיד ב-
`latestGradedStudyYear(courses, currentYear)` ואומרים במפורש איזו שנה מוצגת.
