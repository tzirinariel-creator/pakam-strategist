# פכמון (pakam-strategist) — קרא את זה לפני כל פעולה

אפליקציית תכנון־תואר לסטודנטים של פכ"מ בת"א. **מוצר אמיתי בפרודקשן עם ~24 משתמשים אמיתיים** (pakam-strategist.vercel.app), לא ניסוי. הבעלים: אריאל.

## סדר קריאה חובה בתחילת סשן (לפי הסדר)
1. `docs/חוקת-הפרויקט.md` — מי הבעלים, העקרונות, מה מטריד אותו, איך מדווחים לו. **בלי זה תיכשל גם אם הקוד שלך מושלם.**
2. `docs/תוכנית-ביצוע-סופית.md` — ההחלטות, שלבי הביצוע, קריטריוני־קבלה, החלטות־בעלים.
3. `docs/מפרטי-ביצוע-לפועלים.md` — **גובר על התוכנית בשלבים P/E/S**: קואורדינטות בקוד, 10 כללי־התנהגות, עצי־אבחון.
4. **`docs/רישום-הערות-ראשי.md` — מקור האמת היחיד לסטטוס של כל הערה של אריאל.**
   הערה חדשה נכנסת לשם **מיד, בטקסט של אריאל, לפני שנוגעים בקוד**. ✅ רק עם
   הוכחה של מה שנראה על מסך אמיתי — "הקוד עושה X" אינו הערה סגורה.
5. `docs/משימות-בקרה.md` — יומן הסבבים (ארכיון). מתעדכן בכל סוף פרוסה.

## חוקי ברזל (עוקף כל שיקול אחר)
- `git` ממופה ל־`true` — תמיד `command git`, ואמת HEAD מול origin אחרי push.
- מסד־נתונים אחד = פרודקשן (Supabase). מיגרציות: תוספות בלבד, `npx prisma migrate deploy --config prisma/prisma.config.ts`, לפני קוד שתלוי בהן.
- חשבון הדמו קריאה־בלבד. בדיקות־כתיבה רק ב־vitest.
- פריסה: **`npx -y vercel@48 --prod --yes`**. ה-CLI הלא-מוצמד (`npx vercel`) נשבר על `@vercel/fastify@6.0.0` שלא קיים ברג׳יסטרי, ו-`vercel@46` נכשל על טוקן. על "Not authorized" — נסה שוב פעם אחת. אמת Ready + עמוד 200.
- שער איכות לפני כל commit: **`npm run lint` (0 errors)** + `npx tsc --noEmit` + `npx vitest run` ירוקים + אימות דפדפן + סקירת־דיף עצמית.
  ה־CI ב־GitHub מריץ **lint, tsc, test** — שער שמדלג על lint הוא שער שנכשל ב־CI בלי שנדע. אחרי push: `gh run list --limit 1`.
- **לעולם לא**: לנבא נקודות בידינג / להמציא תאריך או ציון או נתון / להציג נתון בלי מקור.
- אנגלית לא בממוצע התואר; `englishLevel` מוצהר גובר על אמירם (דרך `resolveEnglishLevel`).
- RTL: לעולם לא `dir="ltr"` על טקסט עם מילה עברית — רק `<bdi dir="ltr">` על המספר/תאריך.
- Tailwind v4: אסור `@layer components`; אסור `*/` בהערת CSS; popover/modal תחת אב עם transform → פורטל.
- מונחים: ש״ס (בגרשיים), לא נ"ז; "תחום מיקוד". קופי מוצר ברבים; צ'אט היועץ ביחיד ממוגדר.
- אייקוני AI גנריים (Sparkles וכו') אסורים — רק סמלי המותג (המלך/הרפרנט).
- צילום־מסך של הכלי לא אמין לפריסה — מדוד DOM (`getBoundingClientRect`, הגלאי בתוכנית §0.3).
- החלטות־בעלים (תוכנית §9) — לעולם לא להחליט לבד. כשהערה של אריאל סותרת החלטה מתועדת בקוד — עצור ושאל.
- **שער סגירת ההערות (תוכנית §16):** לפני הכרזת סיום — סגירה הערה־הערה מול `docs/הערות-המקור-של-אריאל.md` (הטקסט המקורי!), כולל כל תתי־החלקים, אל תוך `docs/סגירת-51-הערות.md`. הערה גבולית לא מאשרים לבד — שואלים את אריאל.

## עדכון ידע
בסוף כל פרוסה: עדכן `docs/משימות-בקרה.md`. תובנה חדשה ששייכת לחוקה/תוכנית — הוסף שם, לא רק בצ'אט. **ידע שנשאר רק בשיחה = ידע שיימחק.**

## שער האיכות — `npm run gate` (ולא לקרוא שורות)
**3.9: ה-CI נפל פעמיים על main בגלל טעות קריאה שלי, לא בגלל הקוד.** הרצתי
`npm run lint | tail -2` וקראתי את `"0 errors and 2 warnings potentially
fixable"` כאילו זה מספר השגיאות. זו שורת ה־**auto-fixable**, ו-
`rules-of-hooks` ו-`Cannot access refs during render` אינן כאלה. השורה
האמיתית היא `✖ N problems (X errors, Y warnings)` ו-`tail -2` חתך אותה.

**לכן: `npm run gate`** — מריץ בדיוק את שלושת הצעדים של ה-CI (`npm run lint`
שהוא `eslint .`, `npx tsc --noEmit`, `npx vitest run`) ובודק **קודי יציאה**.
לעולם לא להסיק "0 שגיאות" מפלט חתוך. אחרי push: `gh run list --limit 1`.
