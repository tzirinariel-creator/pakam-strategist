# ממצא פתוח — `bdi "09:00–10:00"` חורג מהמסך

## מה נמדד (לא מה שאני מנחש)
בסיור שלוש הפרסונות, אלמנט `<bdi>` שכל תוכנו טווח שעות נמדד מחוץ לגבול
המסך — `left < -2` או `right > clientWidth + 2` — ו**לא** בתוך מיכל
שנגלל אופקית (הגלאי מדלג על אלה מאז התיקון של 5.9).

| איפה | רוחב | מתי |
|---|---|---|
| תכנון התואר | 1440 | רק **אחרי** לחיצה על "שתף" |
| תכנון התואר | 390 | רק **אחרי** לחיצה על "שתף" |
| יומן | 390 | **מיד**, וגם אחרי "מערכת שעות" |

הגוף עצמו לא גלש באף אחד מהמקרים.

## מה כבר נשלל
- **התרעת החפיפה** — `conflict-detector.ts:148` משתמשת בבידוד יוניקוד
  (`⁦…⁩`) בתוך מחרוזת, לא באלמנט `<bdi>`. לא המקור.
- **`group-row.tsx:106`** — מרנדר `<Bidi text={startTime–endTime} />`,
  אבל בתוך `flex flex-wrap`, שאמור לעטוף.

## מה צריך למדוד לפני תיקון
1. איזה `<bdi>` בדיוק — שרשרת ההורים שלו, ה-`getBoundingClientRect`,
   וה-`overflow` של כל אב.
2. בדיאלוג השיתוף: האם Radix מוסיף פיצוי־סרגל (`padding`/`margin`) לגוף
   ב-RTL, ומזיז את העמוד — כלומר ארטיפקט מדידה ולא באג ויזואלי.
3. ביומן ב-390: צילום מסך שמראה אם משהו באמת נחתך לעין.

**לא תוקן בכוונה.** לתקן פריסה על סמך ניחוש, לילה לפני השקה, בקוד שאני
לא יודע בוודאות שהוא המקור — זה בדיוק מה שאריאל ביקש שלא יקרה.
