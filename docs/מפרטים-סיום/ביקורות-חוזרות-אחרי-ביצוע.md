> ביקורות-יריב שרצו מחדש (8.7 00:00±) מול הריפו **אחרי** שנושאים VI ו-III כבר בוצעו — כלומר, אימות-בדיעבד של הביצוע עצמו.

# נושא VI — ביקורת חוזרת

# ביקורת-יריב — מפרט-סגירה נושא VI

אימתתי כל טענת file:line מול הקוד, הרצתי את חמשת קבצי-הבדיקה (40/40 ירוק, בדיוק כנטען), ובדקתי את שני מסמכי-העוגן. השורה התחתונה: **המפרט מדויק עובדתית באופן יוצא-דופן** — כל הטענות המהותיות על route.ts, gemini-client.ts, exam-facts.ts, exam-planner.ts, exam-planner-content.tsx, study-skyline.tsx, answer-cache.ts, dropdown-menu.tsx, excel-export.ts, globals.css, שאלות-לאריאל (שאלה 14), קו-עיצובי.md:64 ותוכנית-סיום.md:25 אומתו אחת-אחת, כולל הטענה "כל 8 ליקויי-הביקורת הקודמת תוקנו" (בדקתי את כל השמונה מול הקוד — נכון). הקומיטים קיימים בתאריכים המדויקים, main==origin/main. ובכל זאת נמצאו ליקויים אמיתיים:

## ליקויים (חמור → קל)

1. **צעד 2.1 נשען על תנאי-קדם שהוא כרגע שקר: `GEMINI_SHARED_KEY` לא קיים לא ב-`.env.local` ולא ב-`.env`** (אימתתי — grep על שמות-המשתנים בשני הקבצים; המפתח חי רק ב-Vercel). המפרט מנסח זאת כ"לוודא שקיים" — כאילו הבדיקה תעבור — ולא נותן שום נתיב-החלמה. Opus ייתקע כאן בוודאות: הצ'אט ב-dev יחזיר 412 ואין לו מאיפה להשיג את המפתח. **תיקון:** להוסיף לצעד 2.1 הוראה מפורשת — `npx vercel env pull` (לקובץ זמני, להעתיק את המפתח ל-`.env.local`, לא לקמט) — או חלופה: לבצע את אימות-#36 ישירות בפרוד עם משתמש-הבדיקה (המפתח שם קיים ממילא, ונוהל-המחיקה זהה).

2. **תיאור העץ-המלוכלך חלקי ומטעה:** המפרט אומר "יש כרגע rename לא-קומוט של shared-plan" — אבל `git status` מראה **עוד 5 קבצים**: `dashboard-content.tsx`, `planner-content.tsx`, `plan-share.ts`, `middleware.ts` (modified) ו-`share-plan-dialog.tsx` (untracked, חדש). המיטיגציה בצעד 3.2 ("git add על שני הקבצים שנערכו") נכונה ומצילה, אבל מבצע שהוזהר רק מפני rename עלול לחשוב שמשהו דלף כשיראה שישה שינויים זרים. **תיקון:** לתקן את המשפט ל"עבודת יום-2 יושבת בעץ: rename + 4 קבצים ערוכים + קובץ חדש — לא לגעת באף אחד מהם".

3. **אימות-#36 החי כנראה לא יפעיל בכלל את מסלול-החיתוך:** כל הטעם של התיקון הוא שעם תקרת 2000 חיתוך נהיה נדיר — כלומר התרחיש הסביר בצעד 2.4 הוא שהתשובה פשוט תסתיים יפה, ושורת-"נעצרתי" + זרימת-"המשך" + אי-הקאשינג יישארו לא-מאומתים בחי לנצח. המפרט מנסח זאת כ"אם מופיעה השורה…" — תלוי-מזל. **תיקון:** להוסיף תת-צעד: להוריד זמנית `GEMINI_CHAT_MAX_TOKENS` ל-~120 מקומית (שינוי לא-מקומט, להחזיר מיד), לשאול שאלה אחת, לוודא שהשורה מופיעה ו"המשך" ממשיך מאותה נקודה — ואז להחזיר את 2000 ולוודא `git diff` נקי לפני הקומיט.

4. **סיכון 2 נותן עצה שגויה למחצה:** "אם מקבלים 429 — לחכות דקה". מכסות-המפתח-המשותף הן **יומיות** (`windowSeconds: 86_400`, route.ts:175/179) — דקה לא תעזור להן; דקה עוזרת רק ל-rate-limit הנפרד של 20/דקה (route.ts:112-116). מבצע צייתן עלול לחכות-דקה בלולאה מול מכסה יומית. **תיקון:** להבחין — 429 עם ההודעה העברית "העוזר המשותף עמוס" = מכסה יומית (לקרוא Retry-After); 429 עם "Too many requests" = דקתי.

5. **גבול-הסוויפ הטיפוגרפי לא מוצהר:** רשימת 7 המופעים נכונה ומלאה עבור `exam-planner-content.tsx` + `study-skyline.tsx` (אימתתי ב-grep), אבל קו-עיצובי.md:64 אוסר 8/9px "לחלוטין" — ונשארים מופעים צמודי-נושא: `src/components/exam/exam-schedule.tsx:408` (תג מועד ב' בלוח-המבחנים) ו-`src/components/dashboard/exam-countdown.tsx:118`, ועוד ~35 בכלל האפליקציה. אם "מסך-המבחנים" בעיני אריאל כולל את עמוד `/exam` — הרשימה חסרה אחד. **תיקון:** משפט אחד שמצהיר את הגבול ("הסוויפ הוא exam-planner בלבד; `/exam`, הבית והאונבורדינג שייכים לסוויפ-כלל-אפליקציה/נושאים אחרים — סטטוס בקובץ-הבקרה") כדי שההפרות הנותרות לא ייפלו בין הכיסאות.

6. **סיכון 1 מסתיים בקצה פתוח:** "אם גם זה לא מספיק — לעצור" — לעצור ואז מה? להשאיר 9px (הפרת-קו)? לשלוח שאלה? Opus יצטרך להחליט לבד. **תיקון:** "לעצור, להשאיר את המצב הקיים, ולרשום שאלה קצרה בשאלות-לאריאל" — סגירה בת שורה.

7. **סחף-שורות של ±1 בשלושה ציטוטים** (התוכן נכון בכולם): מחלקות-ה-stagger ב-`globals.css:523-528` (לא 524-529); תג-"מומלץ" ב-`exam-planner-content.tsx:321-325` (לא 320-324); התאריך-בכפתור ב-327-329 (לא 326-328). לא מפיל ביצוע — Opus יתאים לפי תוכן — אבל במסמך שמתהדר בדיוק-שורה זה ראוי לתיקון או להערת "התאם לפי תוכן, לא לפי מספר".

8. **הטענה "נפרס" לא מגובה בראיה במסמך** (אין deployment-URL או פלט vercel; רק הקומיטים מוכחים). השפעה נמוכה — צעד 3 ממילא פורס מחדש — אבל מסמך שמביא ראיות לכל השאר צריך ראיה גם כאן, או ניסוח "קומוט ונדחף; הפריסה תאומת בצעד 3".

## מה חסר

שלושה דברים, כולם קטנים. **(1) פרטי משתמש-הבדיקה** — המפרט אומר "נוהל-הבית" בלי לומר איפה הוא מתועד; למזלו `NEXT_PUBLIC_TEST_USER_EMAIL`/`TEST_USER_PASSWORD` קיימים ב-`.env.local`, אז זה עובר — אבל שורה אחת ("קרדנציאלס ב-.env.local; יצירה דרך הרשמה רגילה אם נמחק") הייתה סוגרת את זה. **(2) נתיב-נסיגה לפריסה** — אין אזכור ל-rollback של Vercel אם הסמוק בפרוד נכשל; שורה אחת. **(3) עלות-מכסה של האימות החי** — כל שאלה במבחן-החי שורפת מהמכסה הגלובלית של 800 שגם 24 המשתמשים האמיתיים חיים ממנה; זניח (3-4 בקשות) אבל ראוי למילה. מעבר לזה — סגירות-ההחלטות אמיתית (הסתייגויות-ה"אם" היחידות תחומות והחלטת-האקסל שמורה במפורש לאריאל), אין סתירה לקו-העיצובי או לתוכנית-הסיום (סעיף 8 של "לא עושים" מיושר במדויק לתוכנית-סיום.md:25), ואין מה למחוק — המפרט כבר רזה: שלושה צעדים אמיתיים בלבד, וכל השאר גדרות-הגנה על עבודה חיה. אחרי תיקון ליקויים 1-3 זה מפרט-סגירה שאפשר למסור ל-Opus 4.8 בביטחון מלא.

---

# נושא III — ביקורת חוזרת

# ביקורת-יריב: מפרט נושא III — חברתי

השורה התחתונה קודם: **המפרט נכתב מול snapshot ישן של הריפו, ורוב מה שהוא מגדיר כ"לבנות" כבר בנוי, שוחרר ורץ בפרוד.** זה לא ליקוי נקודתי — זה מפיל את המסמך כולו כמפרט-ביצוע. Opus 4.8 שיריץ אותו כלשונו יעשה נזק, לא תוספת.

## ליקויים (חמור → קל)

**1. [חמור-קריטי] סעיף "מצב קיים" שגוי כמעט כולו — העבודה כבר בוצעה.** אימות מול HEAD:
- טענה א' ("shared-plan מת מאחורי שני קירות") — **שגויה**. `src/middleware.ts:20-22` כבר מכיל `"shared-plan"` ב-PUBLIC_SEGMENTS; העמוד כבר יושב ב-`src/app/[locale]/(public)/shared-plan/` (התיקייה `(protected)/shared-plan` לא קיימת בכלל).
- טענה ג' ("אין אף generateMetadata בכל הריפו") — **שגויה**. `src/app/[locale]/(public)/shared-plan/page.tsx:12-43` מכיל `generateMetadata` מלא, כולל OG ו-`robots: noindex`.
- טענה ד' (""שתף את השבוע שלי" לא קיים") — **שגויה**. `src/lib/week-share.ts` קיים, עם בדיקות (`src/lib/__tests__/week-share.test.ts`) וכפתור חי ב-`src/components/calendar/calendar-content.tsx:168-181`.
- טענה ב' ("כפתור שתף = העתקה עיוורת בלי הסבר-פרטיות ובלי וואטסאפ") — **שגויה**. `src/components/planner/share-plan-dialog.tsx` קיים: דיאלוג Radix עם הסבר מה-נשלח/מה-לא + העתקה + וואטסאפ; `planner-content.tsx:105-111` פותח אותו.
לפי הכלל שהגדרת — טענת-מצב שגויה אחת = ליקוי חמור; כאן יש ארבע. **תיקון:** לגנוז את המפרט ולכתוב **מפרט-דלתא** מול HEAD שמכיל רק את מה שבאמת חסר (ראו סעיף "מה חסר").

**2. [חמור] צעד 3 מוחק פיצ'ר חי ושובר build.** המפרט מורה "להחליף את `src/lib/plan-share.ts`" עם API שמכיל רק `encodePlan/decodePlan/sanitizeName` — אבל הקובץ הנוכחי מייצא גם `rememberSharedPlanReturn`/`consumeSharedPlanReturn` (plan-share.ts:47-70), שנצרכים ב-`dashboard-content.tsx:326` וב-`shared-plan-content.tsx:139,146`. החלפה לפי המפרט = tsc אדום, ואם "מתקנים" לפי רוח-המפרט — מוחקים את לולאת החזרה-אחרי-הרשמה ששוחררה. **תיקון:** לנסח את צעד 3 כהוספת v2 לקובץ הקיים, עם משפט מפורש "הפונקציות rememberSharedPlanReturn/consumeSharedPlanReturn נשארות ללא שינוי".

**3. [חמור] "מה במפורש לא עושים" סותר את הקוד החי ואת המסמך שהוא מצטט.** המפרט אוסר "שימור-מסלול דרך ההרשמה" ומצטט את `docs/תוכנית-סיום.md:25` כ"נדחה במפורש" — אבל: (א) השורה שם היא רשימת "**נדחו מהלילה** במפורש (בקצוות התוכנית, **לא נעלמו**)" — דחייה-בזמן, לא דחייה-עקרונית; (ב) המנגנון כבר מומש (localStorage return, plan-share.ts:38-70) ורץ בפרוד. מבצע צייתן יצטרך לבחור בין הפרת המפרט להסרת פיצ'ר חי מ-24 משתמשים. **תיקון:** למחוק את הסעיף הזה מרשימת ה"לא עושים" ולקבוע: המנגנון הקיים נשאר.

**4. [חמור] רגרסיית-פרטיות: ה-generateMetadata של המפרט מוחק את ה-noindex.** הקוד החי (page.tsx:35) כולל `robots: { index: false, follow: false }` עם הערה מפורשת — קישורי-שיתוף הם אישיים ואסור שיזחלו לגוגל. גרסת המפרט (צעד 6א) משמיטה זאת. ביצוע כלשונו = תוכניות של סטודנטים אמיתיים (בקרוב עם שם פרטי בטוקן!) נכנסות לאינדקס. **תיקון:** לשמר `robots: noindex` בכל גרסה של המטא-דאטה, ולציין זאת כאילוץ-חובה.

**5. [חמור] צעדים 1, 2, 4, 7 בלתי-ניתנים-לביצוע כלשונם — Opus ייתקע מיד.** צעד 1: השורה כבר קיימת (עם הערה אחרת). צעד 2: "להעביר את שני הקבצים מ-(protected)" — התיקייה לא קיימת; וה-layout המוצע עם `AppProviders` יתנגש עם ה-`TRPCReactProvider` שכבר עטוף מקומית ב-page.tsx:54 (שני QueryClients) ויכניס `ThemeProvider` שני מתחת לזה של ה-root. צעד 4: "למחוק את handleShare (שורות 97-114)" — ה-handler הנוכחי כבר פותח דיאלוג. צעד 7: יידרוס את week-share.ts הקיים בחתימה לא-תואמת (`(opts)` במקום `(sessions, opts)` הקיימת), יאבד את טיפול-SATURDAY ואת השימוש בטיפוס `ScheduleSessionData`, וישבור את הקריאה ב-calendar-content.tsx:175. **תיקון:** נגזר מליקוי 1 — מפרט-דלתא.

**6. [בינוני] היפוך החלטה מתועדת-בקוד בלי להכיר בה.** ההערה בראש הדיאלוג הקיים (share-plan-dialog.tsx:19) קובעת במפורש: "**No name**, no gold, no King". המפרט מוסיף שם-פרטי-בהסכמה בלי לציין שהוא הופך החלטה קיימת ולמה. גם היפוך ההיררכיה (המפרט: וואטסאפ ראשי-אינדיגו; הקיים: העתקה ראשית) והחלפת כל הקופי ששוחרר ("רוצה לתכנן ככה את התואר?" → "בונים תוכנית משלכם — חינם") מוצגים כבנייה-מאפס ולא כשכתוב-מודע. **תיקון:** אם שם-בהסכמה עדיין רצוי (זה כן עומד בקו-העיצובי ובפרטיות) — לנסח כ"שינוי מודע להחלטת X, מהסיבה Y", ולהחליט במפורש: הקופי הקיים נשאר או מוחלף.

**7. [בינוני] צעד 3 אינו סגור-החלטות בעצמו.** גוף `decodePlan` v2 ניתן כפרוזה ("... אותו try/catch ואותה ולידציה... ראו הערה") ולא כקוד סגור — בדיוק בנקודה הרגישה ביותר (תאימות v1). גם בצעד 7 יש "ראו הערה" בתוך בלוק-קוד. שאר המסמך דווקא סגור יפה — אין "או"/"כדאי לשקול" אמיתיים. **תיקון:** קוד מלא ל-decodePlan v2.

**8. [קל] זחילת-scope: צעד 8.** הוספת `COMMUNITY_MIN_USERS = 40` בלי שום צרכן = קבוע מת בריפו. ההחלטה על עמוד-המחזור יכולה לחיות במסמך; הקוד לא צריך אותה עכשיו. גם יצירת ה-OG ב-qlmanage (צעד 6ב) גבולית — אבל יש לה fallback סביר, אז עוברת. **תיקון:** למחוק את הוספת-הקבוע מהתכולה; להשאיר את צעד 8 כפסקת-החלטה בלבד. זה לא פוגע באף הערה של אריאל (3, 16 עוסקות בשיתוף והמלצות, לא בקבועים).

**9. [קל] מספרי-שורות וספירות מיושנים לכל האורך.** plan-share "29-55" (בפועל 73-99), YEAR_CONFIG "shared-plan-content.tsx:96" (בפועל 108), calendar "224-279", "290+ בדיקות" (בפועל 335). סימפטום של ליקוי 1, אבל ראוי לציון כי המפרט מתהדר ב"ראיות file:line".

**10. [קל] המפרט שנמסר לי שונה מהמפרט שבריפו.** `docs/מפרטים-סיום/נושא-3-חברתי.md` (467 שורות, מתוארך 7.7, workflow wf_36872991-306) הוא גרסה אחרת של אותו מסמך — שתי גרסאות של "מפרט נושא III" מסתובבות במקביל. מבצע עתידי לא יידע איזו קובעת. **תיקון:** גרסה קנונית אחת בריפו, השנייה נמחקת.

## מה חסר

מה שחסר הוא **הדלתא האמיתית מול HEAD** — ארבעה פריטים קטנים שהם כל מה שנשאר באמת מנושא III, ושהמפרט קובר תחת שמונה צעדים של עבודה-שכבר-נעשתה: (1) פרוטוקול v2 עם שם-פרטי-בהסכמה — תוספת לקובץ הקיים, בלי לגעת בפונקציות-החזרה, כולל צ'קבוקס בדיאלוג הקיים והצגת השם בכותרת העמוד + בכותרת ה-OG (תוך שימור noindex); (2) תמונת `og-shared-plan.png` ייעודית ומספר-הקורסים בתיאור ה-OG (כיום גנרי); (3) `error.message` ב-onError של הייבוא (shared-plan-content.tsx:41 עדיין זורק "ההעתקה נכשלה" גנרי — ההודעה הידידותית של הדמו מ-demo.ts:14-15 נבלעת); (4) ההחלטה-הרשומה על עמוד-המחזור ל-40+ משתמשים. חסר גם: התייחסות לסטטוס בפועל בקובץ-הבקרה (docs/משימות-בקרה.md) — נושא שמסומן "🔬 מחקר-מוצר" בהערות-הסיכום בעוד רובו כבר שוחרר תחת "תוכנית-הסיום" (ההערות בקוד עצמו אומרות "#3/#16 + finish-plan day 2"); מפרט שלא יודע שהעבודה שלו כבר בוצעה הוא הסיכון התפעולי הכי גדול במסמך הזה, כי הביצוע ה"מוצלח" שלו הוא רגרסיה שקטה על פרוד חי.

**קבצים מרכזיים שאומתו:** `src/middleware.ts`, `src/app/[locale]/(public)/shared-plan/page.tsx`, `.../shared-plan-content.tsx`, `src/lib/plan-share.ts`, `src/components/planner/share-plan-dialog.tsx`, `src/components/planner/planner-content.tsx`, `src/lib/week-share.ts`, `src/components/calendar/calendar-content.tsx`, `src/server/routers/plan.ts`, `src/server/routers/course.ts`, `src/server/trpc/demo.ts`, `docs/תוכנית-סיום.md`, `docs/קו-עיצובי.md`, `docs/מפרטים-סיום/נושא-3-חברתי.md`.
