> **מקור:** מחקר-עומק פייבל-5, 8.7.2026 (workflow wf_8fbf1ce5-430). מפרט סגור-החלטות + ביקורת-יריב. **לבצע עם תיקוני-הביקורת.**

# נושא VIII — ערכות-נושא (#49)

I have complete evidence. Here is the deliverable.

---

# נושא VIII-ב: ערכות-עיצוב מעבר לכהה/בהיר (#49)

## מצב קיים (ראיות)

**מנגנון-הנושא הוא מותאם-אישית, לא next-themes.** מצב-הנושא חי ב-Zustand store עם שלושה ערכים בלבד: `"dark" | "light" | "system"` (`src/stores/ui-store.ts:13-14`). ה-`ThemeProvider` רק מוסיף/מסיר class של `dark`/`light` על `<html>` (`src/components/providers/theme-provider.tsx:6-9`). אין שום מושג של "ערכה" מעבר לבהיר/כהה.

**מניעת-הבהוב מוטמעת ב-layout.** סקריפט inline ב-`src/app/layout.tsx:74-78` קורא `pakam-ui` מ-localStorage וקובע `dark`/`light` לפני הרנדר. כל טוקן-נושא נוסף חייב לעבור דרך הסקריפט הזה גם, אחרת יהיה FOUC.

**הבחירה בהגדרות היא שלושה כפתורים.** `src/components/settings/settings-content.tsx:547-584` — grid של 3, מערכת/בהיר/כהה, פעיל = `border-foreground/20 bg-foreground/10`. (הערה: הפעיל כאן משתמש ב-`bg-foreground/10` ולא ב-`bg-accent-brand-muted` — סטייה קלה מקו-עיצובי §7, אבל לא נושא הדיון הזה.)

**תחום-המיקוד (`focusArea`) קיים כשדה-משתמש.** `prisma/schema.prisma:96` — `focusArea String?`. ערכים: `PHILOSOPHY | ECONOMICS | POLITICAL_SCIENCE | ... | UNDECIDED` (`src/lib/constants.ts:38-42`). נבחר בהגדרות (`settings-content.tsx:340`) ובאונבורדינג. **קריטי: זמין רק דרך `profileQuery` בקומפוננטות-קליינט מסוימות** — הוא לא זורם ל-app-shell ולא ל-layout. אין אף מקום שבו `<html>` או ה-body-shell יודעים מה תחום-המיקוד.

**האינדיגו הוא היחיד שמזוהה כ"מותג".** `--accent-brand: #5B5BD6` בבהיר (`globals.css:151`), `#8B8BEA` בכהה (`globals.css:588`). הוא הצבע של המצב-הפעיל בניווט, ה-CTA, פס-ההתקדמות, המספר-העל, ו"שאל את המלך" — כלומר הוא נוגע כמעט בכל מסך.

**צבעי-התחומים כבר קיימים כטוקנים ומיושמים בפועל דרך `DISCIPLINE_CONFIG`, לא דרך `var(--discipline-*)` ישירות.** `globals.css:57-61` מגדיר את חמשת הגוונים ב-oklch. אבל אף TSX לא משתמש ב-`var(--discipline-*)` — הצבע נשלף מ-`DISCIPLINE_CONFIG[course.discipline].color` ומוזרק כ-inline-style (`src/components/planner/course-card.tsx:101,106,156`). כלומר: התחומים כבר "צבועים" בכל מקום שקורס מופיע — פס-קצה, נקודה, tint-רקע.

**הזהב נעול למלך.** `--crown-gold: #c99a3b` / `--crown-gold-bright: #f2c879` (`globals.css:51-52`), ו-`--king-band` מוגדר פעם אחת (`globals.css:54`). קו-עיצובי §1-2 מפורש: זהב = מלך בלבד, רצועה-כהה-אחת = של המלך בלבד.

## החלטות

**החלטה מרכזית: לא בונים ערכות-נושא לפי דיסציפלינה. לא עכשיו, ולא במתכונת שמשנה את גוון-ההדגשה.** זו החלטת-טעם שאני מקבל לפי קו-עיצובי.md, ולהלן הנימוק הכן.

למה לא — שלוש סיבות שכל אחת מהן מספיקה:

1. **זה שובר את "יד אחת מציירת הכול" (עיקרון §5) ואת "האינדיגו הוא הקול" (§1).** קו-עיצובי קובע שהאינדיגו הוא "הצבע הבטוח היחיד של המוצר", והמבחן החמישי ל"מסך מעוצב" הוא: "אפשר לכסות את הלוגו — ועדיין לזהות שזה פקמון". אם המצב-הפעיל בניווט, ה-CTA ופס-ההתקדמות נצבעים באדום (polsci `oklch(0.63 0.2 27)`) אצל סטודנט אחד ובירוק (economics) אצל אחר — אין יותר זהות-מוצר אחת. פקמון הופך לתבנית שמשנה צבע, לא מוצר עם קול.

2. **התנגשות ישירה עם סמנטיקת-הצבע הקיימת.** גוון-הדגשה לפי polsci הוא `oklch(0.63 0.2 27)` — אדום. קו-עיצובי §"סטטוס": אדום = **חסימה בלבד**. סטודנט מדע-המדינה יקבל CTA אדום ומצב-פעיל אדום — בדיוק הצבע שאמור לצעוק "דורש טיפול". וגוון economics ירוק מתנגש עם ה"עומד/הושלם". ערכה-לפי-תחום שוברת את מערכת-הסימון שכל שאר האפליקציה מסתמכת עליה.

3. **ה-ROI שלילי ב-24 משתמשים, וה-focusArea לא זמין היכן שצריך.** התועלת: "נחמד". העלות: (א) להזרים `focusArea` ל-app-shell + ל-layout + לסקריפט-מניעת-ההבהוב; (ב) לגזור 5 ערכות-אינדיגו נגזרות שיעברו ניגודיות-AA על כל רקע; (ג) לתחזק את זה בשתי התוכניות (PPE ו-LLB, ששם התחומים שונים לגמרי — `globals.css:63-68`); (ד) מצב `UNDECIDED` שצריך fallback. זה חוב-תחזוקה קבוע עבור אפקט שאף אחד מ-24 המשתמשים לא ביקש. ההערה עצמה מנוסחת כספק ("**אולי** ערכות-נושא").

**מה כן עושים (מינימלי, לא-שובר):** ליטוש הדארק/בהיר הקיים בשני תיקונים קטנים שמיישרים אותם עם קו-עיצובי, בלי שום טוקן-נושא חדש. פירוט במפרט-הביצוע.

**מה נותנים לתחושת-הזהות-לפי-תחום בלי ערכה:** התחומים כבר נוכחים חזותית בכל מסך דרך `DISCIPLINE_CONFIG` (פסי-קצה, נקודות, tints). זה בדיוק מה שקו-עיצובי §"תחומים" מתיר — "פס-קצה 2-3px או נקודה, לא מילוי מלא". זהו הביטוי-הנכון-היחיד לתחום. אין צורך בעוד.

## מפרט-ביצוע

שני תיקוני-ליטוש בלבד. אין קובץ חדש, אין טוקן חדש, אין שינוי-סכמה.

### תיקון 1 — כפתורי בחירת-הנושא בהגדרות משתמשים באינדיגו לפעיל (יישור לקו-עיצובי §7)

**קובץ:** `src/components/settings/settings-content.tsx`, שורות 547-584 (שלושת הכפתורים system/light/dark).

**מה משתנה:** המצב-הפעיל של כל אחד משלושת הכפתורים משתמש כרגע ב-`border-foreground/20 bg-foreground/10 text-foreground/80`. קו-עיצובי §7 קובע שכל מצב-"אתה כאן"/פעיל בכל המוצר הוא אינדיגו. יש ליישר.

**קוד-לדוגמה (לכל שלושת הכפתורים — אותה החלפה):**
```tsx
// לפני:
theme === "system"
  ? "border-foreground/20 bg-foreground/10 text-foreground/80"
  : "border-border bg-card text-foreground/60 hover:border-foreground/30"

// אחרי:
theme === "system"
  ? "border-accent-brand/30 bg-accent-brand-muted text-accent-brand"
  : "border-border bg-card text-foreground/60 hover:border-foreground/30"
```
להחליף גם ב-`theme === "light"` וב-`theme === "dark"`. שלוש החלפות זהות של אותה מחרוזת.

### תיקון 2 — תווית-הכנות בהגדרות תחום-המיקוד

**קובץ:** `src/components/settings/settings-content.tsx`, שורות 337-338 (הטקסט-הסבר תחת בורר תחום-המיקוד).

**מה לבדוק ולתקן:** לוודא שהטקסט-ההסבר של תחום-המיקוד (`focusAreaHint` + הבלוק הארוך) הוא ≥12px. אם הוא מרונדר ב-`text-xs` (12px) — תקין, אין לגעת. אם מצאת `text-[10px]`/`text-[11px]` על משפט-שלם כאן — להעלות ל-`text-xs`. (קו-עיצובי §3: משפט שלם לעולם לא מתחת ל-12px.) זו בדיקה, לא שינוי ודאי — לפעול רק אם יש הפרה.

### מה שבמפורש **אין** לבנות (חלק מהמפרט)

- **אין** `data-theme="philosophy"` / `data-discipline` / class-לפי-תחום כלשהו.
- **אין** ערך רביעי ב-`ui-store.ts` theme-union.
- **אין** גזירת `--accent-brand` לפי `focusArea`.
- **אין** נגיעה ב-`--crown-gold*`, `--king-band`, או ב-inline-script של layout.
- **אין** כפתור-ערכות נוסף בהגדרות.

## מה לא עושים

- לא בונים ערכות-נושא-לפי-דיסציפלינה (הנימוק המלא ב"החלטות").
- לא משנים את גוון-ההדגשה (accent) לאף משתמש — האינדיגו נשאר יחיד וקבוע.
- לא מוסיפים "ערכת high-contrast" / "sepia" / כל ערכה שלישית — אותו נימוק של יד-אחת; אם ומתי נגישות תדרוש ניגודיות-גבוהה, זה נושא-נגישות נפרד ולא "אופציית-עיצוב".
- לא נוגעים בצבעי-התחומים ב-`globals.css` ולא ב-`DISCIPLINE_CONFIG` — הם כבר עושים בדיוק את העבודה הנכונה (זהות-תחום דרך פס-קצה/נקודה).
- לא מזריקים `focusArea` ל-layout/shell — אין בכך צורך אם לא בונים ערכה.

## בדיקות

1. **ויזואלי — הגדרות/מראה (He + En):** להיכנס ל-Settings, לוודא ששלושת כפתורי-הנושא במצב-פעיל מציגים אינדיגו (`bg-accent-brand-muted` + `text-accent-brand`), והלא-פעילים ניטרליים. לוודא שרק כפתור אחד "דולק" בכל רגע.
2. **מעבר בהיר↔כהה:** ללחוץ כהה → כל האפליקציה עוברת לכהה, האינדיגו הופך ל-`#8B8BEA`, אין הבהוב בטעינה-מחדש (רענון עמוד ב-dark — נשאר dark מיד, בזכות ה-inline-script הקיים).
3. **בדיקת-אי-רגרסיה של המלך:** לפתוח את צ'אט-המלך אחרי התיקון — הכתר/הטבעת עדיין `crown-gold-bright`, רצועת-המלך עדיין כהה. אף שינוי בזהב (התיקון לא נגע בו — בדיקת-שפיות בלבד).
4. **ניגודיות:** בכפתור-נושא פעיל, `text-accent-brand` על `bg-accent-brand-muted` — לאמת קריאוּת בשני המצבים (בהיר: `#5B5BD6` על `rgba(91,91,214,0.08)`; כהה: `#8B8BEA` על `rgba(139,139,234,0.14)`). זהה לדפוס-הסיידבר הקיים, אז אמור לעבור.
5. **תווית-כנות:** להגדיל את הזום ולוודא שהסבר-תחום-המיקוד ≥12px.
6. **tsc + build:** `npm run build` — שינוי-class בלבד, לא אמור לשבור טיפוסים.

## סיכונים

- **סיכון-אפס כמעט:** התיקונים הם החלפת-מחרוזות-class בקומפוננטה אחת. אין לוגיקה, אין סכמה, אין טוקן.
- **סיכון-טעם הפוך (אם *כן* בונים ערכות בעתיד):** לפרוטוקול — אם אריאל יתעקש בכל-זאת על תחושת-תחום חזקה יותר, החלופה הבטוחה-היחידה שאינה שוברת את הקו היא **לא** לשנות accent, אלא להעצים את הנוכחות-הקיימת: למשל נקודת-תחום גדולה יותר ליד כותרת-העמוד בדשבורד, בצבע `var(--discipline-*)` של ה-focusArea. זה נשאר בתוך §"תחומים" (נקודה, לא מילוי) ולא נוגע באינדיגו. אבל גם זאת אני ממליץ לא לעשות ב-24 משתמשים — הנוכחות הקיימת דרך כרטיסי-הקורסים מספיקה.
- **הסיכון האמיתי היחיד הוא הפוך:** שמישהו יקרא את הערה #49 כ"מטלה" ויבנה ערכות. זו תהיה טעות שתעלה ימי-תחזוקה ותדלל את הזהות. ההמלצה הכתובה כאן נועדה למנוע בדיוק את זה.

**קבצים רלוונטיים:** `/Users/Ariel/קלוד קוד ניסוי 6 - אפליקציית פכמ על אמת/pakam-strategist/src/components/settings/settings-content.tsx` (שורות 547-584, 337-338) · `/Users/Ariel/קלוד קוד ניסוי 6 - אפליקציית פכמ על אמת/pakam-strategist/src/stores/ui-store.ts` (ללא-שינוי, לעיון) · `/Users/Ariel/קלוד קוד ניסוי 6 - אפליקציית פכמ על אמת/pakam-strategist/src/app/globals.css` (שורות 51-68, ללא-שינוי) · `/Users/Ariel/קלוד קוד ניסוי 6 - אפליקציית פכמ על אמת/pakam-strategist/src/app/layout.tsx` (inline-script, ללא-שינוי).

---

# ביקורת-יריב

All facts verified. Here are my findings.

---

## ביקורת-יריב — נושא #49 (ערכות-נושא)

**מסקנת-על:** ההחלטה המרכזית (לא לבנות ערכות-לפי-דיסציפלינה) — נכונה, מנומקת היטב, ומגובה בקו-עיצובי. רוב טענות "מצב קיים" מדויקות. אבל יש **שתי טעויות-ציטוט אמיתיות** ובעיה מבנית אחת בתיקון-2 שמנוסח כ"בדיקה" בזמן שהראיה מראה הפרה ודאית. הליקויים החמורים אינם בהחלטה — הם בדיוק ה-file:line וב-scope של הביצוע.

---

### ליקוי 1 (חמור) — תיקון-2 מנוסח כ"אולי", אבל ההפרה ודאית וכבר אומתה
המפרט: *"לוודא שהטקסט... ≥12px. אם הוא מרונדר ב-text-xs — תקין, אין לגעת... זו בדיקה, לא שינוי ודאי."*

הראיה סותרת את הספק. `settings-content.tsx:335` — הבלוק ההסברי הארוך של תחום-המיקוד (משפט שלם, פסקה שלמה בעברית ובאנגלית) מרונדר ב-**`text-[11px]`**:
```
<div className="... p-2.5 text-[11px] leading-relaxed text-foreground/55">
```
קו-עיצובי §64 מפורש: *"משפט שלם ≥ text-xs (12px). text-[8px]/text-[9px] אסורים לחלוטין"* ו-§18: *"לעולם לא מתחת ל-12px"*. זה משפט-שלם ב-11px = הפרה ודאית, לא "בדיקה". שים לב שגם `focusAreaHint` (שורה 330-331) הוא `text-xs` תקין — אבל המפרט הצביע על השורות הלא-נכונות (337-338) בזמן שההפרה בפועל בשורה 335.

**תיקון:** לנסח את תיקון-2 כשינוי ודאי, לא כבדיקה: `settings-content.tsx:335`, החלף `text-[11px]` ל-`text-xs`. הסר את שורות 337-338 מהציטוט (אין שם שום דבר בגודל בעייתי).

---

### ליקוי 2 (חמור) — טעות-ציטוט: §7 אינו "כפתורי בחירה", הוא "ניווט"
המפרט מצדיק את תיקון-1 פעמיים כ*"יישור לקו-עיצובי §7"* ו*"קו-עיצובי §7 קובע שכל מצב-פעיל בכל המוצר הוא אינדיגו"*.

`docs/קו-עיצובי.md:111-113` — §7 כותרתו **"ניווט"**, ומדבר על סיידבר וניווט-תחתון בלבד. הוא אינו קובע ש*"כל מצב-פעיל בכל המוצר"* הוא אינדיגו — זו הכללה שהמפרט ייחס ל-§7 אך אינה כתובה שם. הטענה הנכונה נמצאת ב-**§1** (שורה 9: *"מצב פעיל בסיידבר... כל כפתורי שאל-את-המלך"*) וב-§5 (יד-אחת). כפתורי-בחירת-נושא הם toggle, לא ניווט; ההצדקה שלהם היא עיקרון יד-אחת (§5) + "אינדיגו=הקול" (§1), לא §7.

זו טענת-מצב-קיים שגויה על מסמך-היסוד עצמו. התיקון עצמו (אינדיגו לפעיל) עדיין נכון — רק ההפניה שבורה.

**תיקון:** להחליף "§7" ב-"§1+§5" בשני המקומות בתיקון-1 ובבדיקות.

---

### ליקוי 3 (בינוני) — טעות-ציטוט: `constants.ts:38-42` הוא ה-SLUG_MAP, לא ערכי-focusArea
המפרט: *"ערכים: PHILOSOPHY | ECONOMICS | ... | UNDECIDED (`src/lib/constants.ts:38-42`)"*.

`constants.ts:36-44` הוא `SLUG_MAP` — מיפוי discipline→css-slug, ו**אין בו `UNDECIDED` בכלל**. ערכי-הבחירה בפועל, כולל `UNDECIDED`, מוגדרים ב-`settings-content.tsx:197-202` (`focusOptions`) ונקראים מ-`profileQuery.data.focusArea ?? "UNDECIDED"` (שורה 151). `focusArea` בסכמה הוא `String?` חופשי (`schema.prisma:96`) — אין enum. הצירוף "ערכים... constants.ts:38-42" יוצר enum שלא קיים שם.

**תיקון:** להפנות ל-`settings-content.tsx:197-202` לערכי-הבחירה; להסיר את ההשתמעות שקיים enum ב-constants.

---

### ליקוי 4 (קל) — "DISCIPLINE_CONFIG.color... לא דרך var(--discipline-*)" — נכון-חלקית ומטעה
המפרט טוען שהצבע *"נשלף מ-DISCIPLINE_CONFIG[...].color ומוזרק כ-inline-style... אף TSX לא משתמש ב-var(--discipline-*)."*

מדויק שאין `var(--discipline-*)` ב-TSX. אבל שני דיוקים חשובים: (א) `buildDisciplineConfig` (constants.ts:82) שם `color: d.color` — כלומר הצבע מגיע מ**הגדרת-התוכנית** (`tau-ppe-2025.ts`), לא מטוקני-ה-CSS ב-globals.css:57-61. שני מקורות-אמת לצבעי-התחום (ProgramDefinition ל-TSX, `--discipline-*` ל-Tailwind classes) — וזה **סותר את קו-עיצובי §58** ("*תחומים: אך ורק var(--discipline-*)*") ו-§26 (הנחיתה חייבת להשתמש ב-`var(--discipline-*)`). (ב) `course-card.tsx:101` מזריק `color-mix(... var(--card) ... ${disciplineColor})` — כלומר literal-hex/oklch מ-JS מוזרק ל-inline-style, מה ש§40 אוסר ("hex צבע בתוך TSX — אסור"). המפרט מציג את המצב הזה כ"עובד נכון" בזמן שהוא-עצמו סטייה מהקו. לא נושא-הדיון של #49, אבל הטענה "כבר עושים את העבודה הנכונה" (בחלק "מה לא עושים") אינה מדויקת.

**תיקון:** לרכך ל"התחומים נוכחים חזותית בכל מסך" בלי לטעון שהמנגנון תואם-קו. אפשר לפתוח הערה נפרדת על כפילות-מקור-האמת של צבעי-התחום.

---

### מה שאומת כנכון (בכנות)
- **מנגנון-הנושא מותאם, לא next-themes; union בן 3 ערכים** — נכון (`ui-store.ts:13-14`).
- **ThemeProvider רק מוסיף/מסיר class** — נכון (`theme-provider.tsx:6-9`, מסיר dark+light ומוסיף resolved).
- **inline-script מונע-הבהוב ב-layout** — נכון (`layout.tsx:74-78`, קורא `pakam-ui` מ-localStorage). כל טוקן-נושא נוסף אכן היה חייב לעבור דרכו.
- **שלושת כפתורי-הנושא = `border-foreground/20 bg-foreground/10`** — נכון בול (`settings-content.tsx:548-584`), וזו אכן סטייה מ-§1/§5.
- **`focusArea` לא זורם ל-layout/shell** — נכון: אין hit ב-`src/app/layout.tsx` ולא ב-providers; רק ל-dashboard/planner/catalog **content** (page-level), לא לשורש. הניסוח הזהיר ("`<html>` או body-shell") מדויק.
- **אינדיגו `#5B5BD6`→`#8B8BEA`** — נכון (`globals.css:151,588`).
- **זהב נעול למלך, `--king-band` פעם אחת** — נכון (`globals.css:51-54`), תואם §1-2.
- **polsci=אדום (hue 27), economics=ירוק (hue 152) מתנגשים עם סמנטיקת-סטטוס §59** — הטענה נכונה ומשכנעת. hue 27 באמת אדום, ו§59 קובע אדום=חסימה-בלבד. זה הנימוק החזק ביותר במפרט והוא עומד.

**סגירות-החלטות ("או"/"אפשר"):** בדקתי. ה"אפשר"-ים במפרט הם ציטוטי-UI ("אפשר להתלבט בשנה א'") או תיאורי-חלופה בסיכונים — לא סגירות-החלטה פתוחות. ההחלטות עצמן נעולות היטב (רשימת "מה שבמפורש אין לבנות" מצוינת). **אין זחילת-scope** — להפך, המפרט הוא anti-scope-creep במכוון, וזה הצד החזק שלו.

**סיכוני-רגרסיה על 24 משתמשים:** נמוכים כנטען. שינוי-class בקומפוננטה אחת. בדיקת-אי-הרגרסיה של המלך (בדיקה 3) היא שפיות-בלבד ומיותרת-אך-לא-מזיקה. **איפה Opus 4.8 ייתקע:** בתיקון-2 — אם ינווט לפי הניסוח "אם text-xs אל תיגע" הוא עלול **לפספס את ההפרה בשורה 335** כי הפנו אותו לשורות 337-338 הנקיות. חובה לתקן את ה-file:line (ליקוי 1) אחרת התיקון היחיד בעל-ערך-ויזואלי-אמיתי לא ייושם.

---

### מה חסר
1. **בדיקת-ניגודיות של תיקון-2 אחרי השינוי** — כשמעלים ל-`text-xs`, הבלוק גדל; אין וידוא שהוא לא דוחף את ה-Select מתחת לקיפול במובייל. זניח, אבל להזכיר.
2. **מה קורה כשמשתמש ב-`system` וה-OS מתחלף בזמן-אמת** — לא בבדיקות. `theme-provider.tsx:33-45` מטפל בזה עם `matchMedia listener`, אבל אין בדיקה שהאינדיגו-הפעיל בכפתור-`system` לא "מהבהב". קל.
3. **אין הפניה ל-en.json/he.json** — התיקון נוגע במחרוזות-class בלבד, אבל בדיקה 1 דורשת He+En; שווה לציין שאין שינוי-תרגום, כדי ש-Opus לא יחפש מפתחות.
4. **ליקוי-4 (כפילות מקור-האמת של צבעי-תחום, וסטיית §40/§58)** אינו מוזכר כלל כחוב — המפרט מציג את המצב הקיים כתקין. אם רוצים, זו הערה-נפרדת לגיטימית לגל הבא.

**קבצים רלוונטיים שאומתו:** `src/components/settings/settings-content.tsx` (335, 548-584, 197-202), `src/stores/ui-store.ts:13-14`, `src/components/providers/theme-provider.tsx:6-9`, `src/app/layout.tsx:74-78`, `src/app/globals.css:51-61,151,588`, `src/lib/constants.ts:36-44,82,138`, `prisma/schema.prisma:96`, `docs/קו-עיצובי.md:9,58,59,111-113,64,40`.
