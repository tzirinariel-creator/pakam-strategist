# סטטוס חי — עדכן אחרי כל צעד

> נכתב כדי שאפשר יהיה להמשיך בדיוק מכאן אחרי ניתוק/מכסה.
> **עדכן את הקובץ הזה אחרי כל פרוסה, לפני שממשיכים.**

## איפה אנחנו (2.9, ~05:10)

**מצב הרשת:** main = כל העבודה שלי, דחוף. ענף `feature/apple-design-update`
של הסשן השני = הסרטון + מעבר ניגודיות של 871 החלפות. **אומת אצלי: tsc נקי,
0 שגיאות lint, 2193 בדיקות עוברות.** המיזוג הוא fast-forward נקי. חסום לי
להריץ — אריאל צריך להריץ:
```
command git merge --ff-only feature/apple-design-update && command git push origin main
```

**שני וורקפלואים נכשלו על מכסת סשן** (0 תוצאות): אימות ההערות הקודמות,
ואבחון הערות 2.9. **לא להריץ אותם מחדש כסוכנים** — לעשות את העבודה ישירות.
זה גם מהיר יותר וגם לא שורף מכסה.

## מה בוצע ונדחף (main)

| קומיט | מה |
|---|---|
| 83b7bb8 | הטבלה של אריאל הכריעה — 2 קורסי חובה הופעלו, שער 101 |
| 0b15b2e | 3 מספרים שקריים על המסך; שומר הקטלוג עוגן לטיפים |
| aa5a56e | רשימה אחת במקום שלוש + ניקוי 5 קבצים יתומים; דף נחיתה |
| d1b91e8 | סקיל HIG: 10 כשלי נגישות נמדדו ותוקנו; הנגשת הסימולציה |
| 48e62b3 | הערות 2.9 של אריאל נשמרו מילה במילה |
| 331008a | כל שגיאת שרת דיברה אנגלית — עברית היא ברירת המחדל |

## מה פתוח — לפי סדר עדיפות

### 1. הליכה כמשתמש עם צילומי מסך  ← **גולת הכותרת, בתהליך**
דמו מחובר, 375px, `/he/dashboard`. הסשן נקטע כאן.
**איך ממשיכים:** `preview_start {name:"pakam-dev"}` → `navigate` ל-
`http://localhost:3000/he/login` → `read_page` → קליק ב-ref על "נסו דמו".
אם הפאנל תקוע: `javascript_tool` עם `.click()`.

**נמצא עד כה:**
- ✅ מסך התחברות תקין ב-375px, שלוש הנקודות מיושרות
- ❗ **הערה #1 אושרה בעין:** באנר "חדשים בפכ״מ?" בראש הדשבורד — הכותרת
  נשברת ל-3 שורות, כפתור "למדריך" מרחף באמצע עמודת הטקסט, ה-× לצידו,
  התיבה גבוהה מדי. בדיוק "הטקסט למעלה והמשצבת גדולה".
- ℹ️ **הערה #10 נענתה:** אין טאב למלך. הטאב-בר: בית / תכנון התואר / יומן /
  דרישות התואר. המלך הוא כפתור צף בלבד.

**מה עוד לא נבדק בעין:** כל שאר המסכים.

### 2. אימות ההערות הקודמות
לעשות ידנית מול `docs/רישום-הערות-ראשי.md`. סטנדרט: לא "הקוד עושה X" אלא
"אדם שעושה Y רואה Z".

### 3. הערות 2.9 (docs/הערות-אריאל-2.9-סבב-משתמש.md)
חוסמי השקה: איפוס שנה/סמסטר ל-5%, הלופ אחרי שמירה, "לא הצלחנו לטעון".
מוצר: תכנון שני סמסטרים של השנה הנוכחית.

## כללי עבודה שנלמדו בסשן הזה
- **אל תריץ `git add -A` בזמן שסוכנים כותבים לתיקייה.** ככה נדחף רפקטור
  שלא נקרא.
- לפני שמדווחים על באג בדפדפן — לוודא שזה לא הסביבה. `ERR_BLOCKED_BY_CLIENT`
  היה תוסף שלי, לא תקלת פרודקשן.
- וורקפלו עם סוכן-מקדים יחיד = נקודת כשל יחידה. לפצל לפי מסמך/נושא.
