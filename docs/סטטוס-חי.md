# סטטוס חי — עדכן אחרי כל צעד

> קיים כדי שאפשר יהיה להמשיך בדיוק מכאן אחרי ניתוק או סוף מכסה.
> **עדכן אותו אחרי כל פרוסה, לפני שממשיכים הלאה.**

עודכן: 2.9, ~14:05

---

## מצב הרשת

`main` מחזיק את כל העבודה, הכול דחוף.

ענף `feature/apple-design-update` של הסשן השני מחזיק את הסרטון ומעבר
ניגודיות של 871 החלפות. **אומת אצלי: tsc נקי · 0 שגיאות lint · 2,193
בדיקות עוברות.** המיזוג הוא fast-forward נקי. חסום לי להריץ — אריאל:

```
command git merge --ff-only feature/apple-design-update && command git push origin main
```

הסרטון: `.claude/worktrees/feature+apple-design-update/video/out/pakam-promo.mp4`
(41.8 שניות · 1080×1920 · 11 סצנות)

---

## מהערות 2.9 — מה נסגר

| # | ההערה | מה נמצא | קומיט |
|---|---|---|---|
| **1** | "הטקסט למעלה והמשצבת גדולה" | נמדד: עמודת טקסט 123px מתוך 327. `min-w-0 flex-1` בלי בסיס → הדפדפן מוחץ את הטקסט במקום להעיף את הכפתור לשורה הבאה. 14 כרטיסים. אחרי: 241px, גובה 188→164 | `da803a9` |
| **8** | "בידינג במקום המכרז" + "שנה 2" | 21 מופעים הוחלפו; "מכרז" נשאר רק בהגדרות ובמילות-חיפוש. YEAR_CONFIG במקום הספרה. גם "להתמכרז", שאינה מילה | `b1ed67b` |
| **13** | הכוכבים בקטלוג | 123/304 בלי תחום, 61/67 סמינרים. **הצלבתי מול הידיעון: אפס סתירות** — הוא מפרט סמינרים לפי ש״ס. לא נגעתי במסד. הבאג האמיתי: הקטלוג התעלם מהשיוך האישי שכל שאר האפליקציה מכבדת | `eee865b` |
| **12** | "המחשבון המוזר והשבור" | נפתח על יעד 80 קבוע וסרגל 60–100. ב-97.3 אמר "נדרש 55.3". עכשיו נפתח נקודה מעל הממוצע, הסרגל לא יורד מתחתיו, ויש שורת ייחוס. אומת: 87.0 → סרגל 87, יעד 88, נדרש 91.8 | `d3adeec` |
| **2** | "זה קפץ לאנשהו" | הקישור מהבידינג לתכנון היה `/planner/semester` בלי `/he` — המסך היחיד שייבא `next/link` הגולמי. middleware מפנה → טעינת עמוד מלאה → הראוטר נהרס → המתכנן על ברירות מחדל | `4d755c4` |

**#2 עוד לא שלם: "5 אחוז מהתואר" לא הוסבר.** בדקתי את savePlan — היקף
המחיקה שם מסוכן במבנה (מוחק כל שורה מתוכננת שלא במטען), אבל הלקוח זורע
יותר ממה שהשרת מוחק, ויש שער טעינה ושער שגיאה לפני שהלוח עולה. לא שם.

---

## מה פתוח, לפי סדר

### 1. חוסמי השקה שנשארו
- **#5 הלופ** — "הכול מוכן" ↔ "המלך טוען", עד שנתקעים ואי אפשר ללחוץ בית
- **#17 / כשל טעינה** — "לא הצלחנו לטעון את התוכנית" + מחיקת חשבון נכשלת

### 2. מוצר
- **#3 שני סמסטרים של השנה הנוכחית** — ההערה שהוא סימן כקריטית
- **#6** תכנון מבחנים פר-סמסטר · **#7** להבליט את האקסל
- **#9** בינארי: לראות השפעה לפני שמחליטים
- **#10** אין טאב למלך (אומת: הטאב-בר הוא בית/תכנון/יומן/דרישות)
- **#11** עוד שאלות לדוגמה למלך

### 3. הליכה כמשתמש — ממשיכה
דמו מחובר עם תוכנית מלאה (שנה ב׳ סמסטר ב׳). נבדקו בעין: התחברות, דשבורד,
בידינג, מחשבון ציון גמר. **לא נבדקו:** מתכנן, תיק אקדמי, מבחנים, יומן,
קטלוג, שושלת, מילואים, הגדרות, אונבורדינג מאפס.

**איך ממשיכים:** `preview_start {name:"pakam-dev"}` → `navigate` ל-
`/he/login` → `read_page` → קליק ב-ref על "נסו דמו". אם הפאנל תקוע —
`javascript_tool` עם `.click()`.

### 4. אימות ההערות הקודמות
ידנית מול `docs/רישום-הערות-ראשי.md`. **שני וורקפלואים נכשלו על מכסה —
לא להריץ אותם שוב כסוכנים, לעשות ישירות.**

---

## מלכודות שנלמדו כאן

- **עומס מערכת מפיל בדיקות.** בעומס 48 (הסשן השני רץ) נופלות 10–20 בדיקות
  בטיים-אאוט של 5 שניות. כל קובץ עובר לבד. הפתרון:
  `npx vitest run --testTimeout=30000` — ככה כל 2,202 עוברות.
  **אל תתקן "כשל" לפני שבדקת את זה.**
- **אל תריץ `git add -A` בזמן שסוכנים כותבים לתיקייה.** ככה נדחף רפקטור
  שלא נקרא.
- **לפני שמדווחים על באג בדפדפן — לוודא שזה לא הסביבה.**
  `ERR_BLOCKED_BY_CLIENT` היה תוסף שלי, לא תקלת פרודקשן.
- **וורקפלו עם סוכן-מקדים יחיד = נקודת כשל יחידה.** לפצל לפי מסמך/נושא.
- **עריכת מסמך בהחלפת-מחרוזת נכשלת בשקט.** שתי עדכוני סטטוס לא נתפסו כי
  העוגן לא התאים. לוודא אחרי כל עריכה.
