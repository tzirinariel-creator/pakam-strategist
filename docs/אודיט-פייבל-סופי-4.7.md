# 🔍 אודיט-העומק הסופי של פייבל 5 (#23) — תוצאות

אודיט אדוורסרי רב-סוכן (workflow) על **כל מה שנוסף מאז הקומיט `7358187`** (46 קבצים): 11 עדשות מוצאות → מיזוג → אימות-מפריך (2 מפריכים + שובר-שוויון לכל HIGH/MED) → מבקר-שלמות. **15 ממצאים אומתו** (8 הופרכו מתוך 27 גולמיים). **כולם תוקנו, נדחפו ועלו לפרודקשן.**

> **הערה על המודל:** שתי העדשות הקריטיות לאבטחה (`security-api`, `prompt-injection`) רצו על **פייבל 5** לפני שנגמרה המכסה; שאר 9 העדשות הושלמו על **אופוס 4.8** (המכסה של פייבל מתאפסת 6.7). היתרון של פייבל — תפיסת סתירות בין-מערכות — מומש בדיוק בממצא ה-HIGH הכי חשוב (הזרקת-פרומפט).

## 🔴 HIGH (6)

| # | הממצא | התיקון |
|---|-------|--------|
| deterministicHint | ה-route של הצ'אט קיבל hint מגוף-הבקשה והזריק אותו לסיסטם-פרומפט **כסמכות עליונה, אחרי כלל-הבידינג** → hint מזויף יכול לגרום למלך להציג תחזית-נקודות. | מסגור-מחדש (נתון, לא הוראה; בטיחות+שרת גוברים) + שער-שרת `isSafeDeterministicHint` שזורק hint שמצרף מילות-בידינג + מספר דו-ספרתי. **(73131d8)** |
| סורק-ציונים | שם-קורס "מורחב" בלי קוד (למשל "…המתקדם") נקשר לקורס-הבסיס, וכיוון שהשורה **מסומנת-אוטומטית**, לחיצה אחת דרסה ציון-עובר והפכה אותו ל"נכשל". | התאמות-הכלה הן עכשיו "fuzzy" נפרד — **לא-מסומנות**, עם אזהרת "ודאו שזה הקורס"; שברים-קצרים לא מתאימים; חזרות (אותו קוד) מסומנות "ambiguous". |
| המלך — בינארי | סטודנט **לא-מילואימניק** קיבל "כהטבת מילואים נשארו לך 5 המרות" (נפילת ה-cap האוניברסלי). אותו class כמו תיקון כרטיס-הבינארי, אבל ב-degree-qa. | שער בשורש (`use-qa-context`: 0 ל-NONE) + בַּמטפל (דורש `miluimGroupName`). |
| תמונה בצ'אט | (א) התמונה שנשלחה הוצגה שבורה — `clearImage()` ביטל את ה-object-URL ששמור בהודעה; (ב) שאלה עם-תמונה-בלבד לא נשלחה (Send מושבת על טקסט ריק). | (א) thumbnail נשמר כ-data-URL יציב; (ב) Send פעיל כשיש תמונה מצורפת. |
| ממוצע בתיק | הממוצע בכרטיס-הסיכום **כלל קורסים בינאריים** — סתר את /graduation, יועץ-הבינארי והמלך באותו מסך. | מסנן `!isBinary` — תואם את השאר. |
| סורק-סילבוס | תאריכים בלתי-אפשריים (30 בפבר׳) עברו ולידציה ו-`Date()` גלגל אותם ליום אחר מזה שהסטודנט אישר. | בדיקת round-trip קלנדרית. |

## 🟡 MEDIUM (3)
- **סנכרון:** `course-card` (הסרה/עדכון) לא ריענן את `checkCompliance` — אוחד ל-`invalidatePlanData` (זה הפער שנשאר מ-#23!).
- **נגישות:** תיבות-הסימון בסורקים בלי שם-נגיש → נוספו `aria-label`.
- **בידינג:** העתקת-הסיכום ללא `catch` → כשל-clipboard מציג טוסט "העתק ידנית".

## 🟢 LOW (6)
- שגיאות-ספק בסורקים: 400/403 → "מפתח נדחה", 404 → "לא זמין" (במקום "צילום לא ברור" ששורף מכסה).
- PDF גדול מדי → הודעה מתורגמת "צלמו את העמוד" לפני העלאה.
- סריקה חוזרת של סילבוס לא מכפילה משימות (דילוג על כותרת+יום קיימים).
- HEIC הוסר מרשימת-הצירוף של המלך (לא ניתן לתצוגה בדסקטופ).
- תוויות סוג-מפגש בבידינג מתורגמות ל-EN.
- קודי-קורס כפולים (חזרות) בסורק — טופל יחד עם ה-HIGH.

## ✅ אימות
tsc נקי · **252 בדיקות** (+6 חדשות שמכסות בדיוק את ה-repro של כל HIGH) · lint 0 שגיאות · CI ירוק · בדיקה-חיה: אפס שגיאות-console, לוגיקת Send-מושבת נכונה, HEIC נעלם מרשימת-הצירוף · **פרודקשן חי**.

## מה שהאודיט אישר כתקין (לא-ממצא)
אפס-תחזיות-בידינג נשמר; הדמו קריאה-בלבד; Gemini חינמי; שכבת-ה-CSS `pakam-ui`; ה-hydration של MiluimSection. 8 ממצאים גולמיים הופרכו באימות (למשל "אין hardening נגד תוכן-לא-מהימן בסורקים" — הופרך: הפריטים עוברים אישור-פר-שורה של הסטודנט).
