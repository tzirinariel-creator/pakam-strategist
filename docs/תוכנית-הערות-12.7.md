# תוכנית-ביצוע: הערות אריאל 12.7 (סשן הבוקר, 45 הערות + 5 צילומי-מסך + אקסל)

> **מסמך-עוגן.** נכתב אחרי ניתוח-עומק של כל הערה, הצילומים, הקוד והאקסל של החברה.
> כל סעיף: מה אריאל אמר → מה השורש בקוד → התיקון המדויק → איך מאמתים.
> מבצע שקורא רק את המסמך הזה חייב להצליח. סדר הביצוע: לפי האצוות בסוף.
> חוקי-הברזל של CLAUDE.md חלים על הכול (command git, שער-איכות מלא, אין המצאת נתונים, RTL, pg() ליחיד-ממוגדר).

## עובדות-בסיס שנבדקו (אל תבדוק שוב)
- `User.firstName` + `User.lastName` **כבר בסכמה** (schema.prisma:92-93). אין מיגרציה.
- טבלת `MiluimSemester` פר-סמסטר **כבר קיימת** (schema.prisma:133-155). אין מיגרציה.
- ממוצעי-קטלוג = **ארזים** (נתונים אמיתיים, cron שבועי). לא המצאה — אבל המקור לא מוצג בשם.
- הסורק = Gemini vision בלבד (`api/ai/scan-grades`), אין פרסר-טקסט ל-PDF. ה-fixture בריפו סינתטי (מבנה אמיתי, ציונים לא).
- מהצילומים של הבוקר (הרשמה מחדש שלו, שנה א׳+סמסטר ב׳, לשון זכר, מילואים 100 ימים קבוצה C):
  הסריקה החליפה ציונים בין שורות (לוגיקה 89 במקום 100), השאירה את קריאה מודרכת א׳ ריק.
- האקסל של החברה (`תכנון תקופת מבחנים.xlsx`): **לוח-שנה שבועי** — עמודות=ימי-שבוע, שורות=שבועות,
  כל תא = המקצוע שלומדים באותו יום, ימי-מבחן מסומנים "X מועד א׳", ימי-הוראה מסומנים "סמסטר".
  זו התבנית לייצוא/תצוגה החדשים (#35/36).

## עקרונות-הבדיקה של אריאל (להפנים — ולבדוק כך כל דבר בעתיד)
1. עובר כמשתמש אמיתי עם הנתונים האמיתיים שלו, מסך-מסך, וקורא כל מילה.
2. כל שורת-קופי חייבת להישמע אנושית. סימני-AI: "בטוח להגיש", חיצים לכיוון הלא-נכון ב-RTL, ניסוחים גנריים, הסברים שנשמעים כמו תרגום.
3. אם המוצר "אישי" — הכול אישי: מין (pg בכל /ת, /ה), שם, שנה, מצב. חצי-אישי = שבור.
4. שום דבר שאפשר לגזור מנתונים לא ממולא ידנית (בינארי, ש״ס-פטור, קבוצת-מילואים).
5. זרימה חייבת להיות הגיונית בזמן אמת ("היום 12.7 — למה מתכננים לי את הסמסטר שנגמר?").
6. אסור להמציא נתונים, ואם מציגים נתון — שם-מקור ליד.
7. פיצ'ר שהוא "בערך" (סימולציית-בידינג על קורסים שכבר יש) גרוע מאין-פיצ'ר.

---

## ההערות — הבנה, שורש, תיקון

### א. אמת-הנתונים (הכי קריטי)

**#5 הסורק כתב 89 בלוגיקה במקום 100 (+#4א לא סרק את קריאה מודרכת א׳)**
- שורש: קריאת vision יחידה של flash-lite על טבלה צפופה RTL — מחליף ספרות בין שורות סמוכות ומפיל שורות. אין אימות.
- תיקון (חבילה, `src/lib/grade-sheet.ts` + `api/ai/scan-grades/route.ts` + `src/components/record/*scan*`):
  1. הפרומפט (GRADE_SHEET_SYSTEM): להוסיף חוקי-שורה קשיחים ("הציון באותה שורה של שם-הקורס; קרא שורה-שורה; אל תדלג; החזר גם את הממוצע המודפס בשדה printedAverage").
  2. **סריקת-אימות שנייה**: קריאה שנייה לוידוא (אותה תמונה+התוצאה, "בדוק ותקן"), השוואת שתי הריצות שורה-שורה.
  3. שורות שבהן הריצות חלוקות → דגל `uncertain: true` → ב-UI מסגרת ענבר + "ודאו מול הגיליון".
  4. **בדיקת-ממוצע**: ממוצע-מחושב מהשורות מול printedAverage (אם הוחזר) — סטייה >0.5 ⇒ באנר אזהרה למעלה.
  5. שורה של קורס שכבר בתוכנית וחזרה בלי ציון ⇒ שורת-הנחיה "בגיליון יש ציון ל-X? הזינו ידנית".
- אימות: unit על ההשוואה/דגלים; סריקה חיה שלו תראה דגלים במקום טעות שקטה.

**#4ב סיכום-AI אישי בסוף סריקה**
- אחרי אישור-הסריקה: כרטיס מסכם (דטרמיניסטי, לא LLM — אמין ומיידי): ממוצע חדש, כמה קורסים עודכנו, נכשלים אם יש, פער-בולט אחד. שפה אישית ממוגדרת. קובץ: רכיב הסריקה + grade-calculator.
- אימות: סריקת-דמו + unit.

**#44 "אתה ממציא ציונים בקטלוג?"**
- לא — ארזים. אבל חסר שם-מקור. תיקון: בכרטיס-קורס ובמודל: "נתוני ארזים (סטטיסטיקות אמת של סטודנטים, שנה X) — לא ציון רשמי". לבדוק ש-seed.ts לא ממציא averageGrade (אם כן — לאפס).
- קבצים: catalog-content.tsx:140-164, course-detail-modal.tsx:101-120.

### ב. הסשן הראשון (הרשמה→תכנון→שמירה)

**#1 שם פרטי + שם משפחה**
- schema קיים. תיקון: step-profile — שדה שם-משפחה (רשות) ליד השם-הפרטי; settings — שדה עריכה; שימושים חדשים: כותרת-אקסל ("התוכנית של אריאל צירין"), שיתופים, ברכת-בוקר בדשבורד, "דבר הרפרנט". updateProfile כבר מקבל? לוודא.

**#2 טופס 3010 בהרשמה**
- במקום שאלות ידניות במסך-המילואים בהרשמה: בלוק "יש לכם טופס 3010? העלו — נקרא לבד" (משתמש ב-parse הקיים של settings/form-3010) → חלון-אישור של מה שנקלט (ימים לכל סמסטר, קבוצה מחושבת) → ממלא אוטומטית. fallback ידני נשאר.
- קבצים: onboarding step-profile (miluim section) / רכיב 3010 קיים ב-settings — לחלץ לרכיב משותף.

**#3+#22 הסברי-מילואים נשמעים AI/שבורים; "מה זה ב׳ ג׳ ז׳?"**
- לשכתב את כל בלוק-ההסבר (onboarding + settings + planner): בלי רשימת-מקף יבשה; להסביר קודם מה הקבוצות (א=10-19 ימים, ב=20-34, ג=35+, ז=מסלול מיוחד — לבדוק את ההגדרות האמיתיות ב-miluim.ts!) בעברית של בן-אדם, ואז מה מגיע. כל מופע "קבוצה C" באנגלית → עברית עקבית.

**#6 "ש׳ מגע · מבחנים צפופים" קופץ בכרטיס-הסיכום**
- לבדוק את LEVEL_LABELS ב-calculateHonestLoad; התווית מופיעה בלי הסבר בכרטיס הראשון. תיקון: תווית ידידותית + tooltip קיים → להפוך לשורת-משנה מוסברת ("6 ש׳ מגע שבועיות — עומס מאוזן"), ולא להציג אזהרת-צפיפות-מבחנים בכרטיס-האישור הראשון אלא רק בעריכה.

**#7 תכנן ישר את סמסטר ב׳ (שנגמר!)**
- שורש: ההרשמה שואלת "באיזה סמסטר אתם" ומתכננת אותו, בלי מודעות לתאריך (12.7 = הסמסטר נגמר).
- תיקון: יעד-התכנון נגזר מהלוח האקדמי: אם היום אחרי סוף-ההוראה של הסמסטר המוצהר → מציעים את הסמסטר *הבא* עם שורה מסבירה ("סמסטר ב׳ הסתיים — בואו נתכנן את שנה ב׳ סמסטר א׳"). onboarding-wizard + semester-planner target.

**#8 מסך-השמירה נתקע + המלבן הצבעוני לא מגיע לקצוות**
- לחקור את ה-overlay של השמירה (step-ready/planner save): להוסיף timeout עם retry + הודעת-שגיאה אמיתית במקום היתקעות; "המשיכו בלי שמירה" רק אחרי X שניות.
- הרקע: gradient שלא מכסה את כל ה-viewport (נגמר לפני הקצוות) — לתקן ל-fixed inset-0 מלא, ולבדוק בכל מסכי-ה-overlay באפליקציה.

**#17 (+B1) "לא באמת ראיתי את התכנון לפני שאישרתי"**
- הרשימה נוספה אתמול (semester-summary). לחזק: LiveTimetable מוקטן בתוך כרטיס-האישור (יש כבר preferGrid/coursePreview), כפתור-משנה בולט "הצגת המערכת המלאה". לאמת חי.

**#41 המערכת מציגה את כל הקבוצות של קורס בלי שנבחרה קבוצה**
- שורש: בחירת-קבוצה לא נאכפת/לא נשמרת לקורסים שמגיעים מהיסטוריה/סריקה; היומן מציג את כל ה-sessions.
- תיקון: (א) ביומן — קורס עם כמה קבוצות ובלי בחירה ⇒ באדג' "בחרו קבוצה" + מודל בחירה במקום להציג הכול; (ב) בזרימת-התכנון — בחירת קבוצה חובה-רכה (מוצע group 01 עם אפשרות שינוי); (ג) persist ב-userCourse.groupNumber (לבדוק אם קיים שדה — אם לא, לשמור בהעדפת-plan הקיימת).

### ג. המלך (איכות שיחה)

**#10 לא אישי/אדפטיבי; לא היה אקטיבי על "סיימתי קורס אנגלית מתקדמים ב׳"; דיבר על סמינרים לשנה א׳**
- 3 תיקונים:
  1. action-router: לזהות השלמת קורס-אנגלית לפי רמה ("סיימתי (את )?(קורס )?אנגלית X" → הקורס-האנגלית הפעיל בתוכנית) + intent הוספה עתידית ("אעשה הסמסטר חשבונאות" → כרטיס הוספה).
  2. תשובת-היכולות הדטרמיניסטית (degree-qa "איך אתה יכול לעזור"): לסנן פערים לפי שנה — שנה א׳ לא שומעת על סמינרים (שנה ב׳+) ולא רשימת-כביסה; 2-3 פערים רלוונטיים-עכשיו + הצעה אחת.
  3. mentor-prompt: הנחיה "התאם את התשובה לשנת-הלימוד; אל תציף דרישות שרלוונטיות בעוד שנים".

**#30 שאלת-המשך מקבלת תשובה קנויה לא-קשורה**
- שורש: answer-router מפעיל דטרמיניסטי גם על המשכים ("יש עוד מגבלות על הבינארי?").
- תיקון: זיהוי-המשך (יש היסטוריה + השאלה קצרה/אנפורית: מתחילה ב"ו", "יש עוד", "וגם", "ומה עם", מכילה כינוי-רמז בלי נושא) ⇒ דילוג על הדטרמיניסטי, ישר LLM עם ההיסטוריה.

**#11 טון מלך-פילוסוף אמיתי**
- mentor-prompt: פתיח-אישיות מחוזק — שקול, חכם, לעיתים ציטוט קצר (אפלטון/אריסטו/מיל) *רק כשמתאים*, מטאפורות-מסע ("התואר הוא פוליס — בונים אותה אבן-אבן"), בלי קיטש, תמיד מעוגן בנתונים. + 3 דוגמאות-סגנון בפרומפט. גם תשובות degree-qa הקנויות מקבלות ניסוח בטון.

**#25+#29+#12+#14+#33 עברית שבורה (דוגמאות שנתן)**
- "בטוח להגיש למכרז" → "אין חפיפות בין הקורסים שבחרתם — אפשר להגיש את הבקשה בראש שקט".
- חץ הפוך בבידינג → "אם תגישו בקשה לקורס ב׳ שחופף לקורס א׳ שכבר שלכם — הרישום לא׳ יבוטל אוטומטית. בדקו חפיפות לפני ההגשה."
- "כשהציונים יוצאים — סמנו כאן" → "אלה הקורסים של הסמסטר. כשיתפרסם ציון — מסמנים ✓ ומזינים אותו כאן."
- "עומד/ת בכל הכללים" → pg() (#16).
- מועדי-מילואים → "כמילואימניק מותר לך לגשת ל-2 מתוך 3 מועדים, והציון הגבוה מביניהם נשמר אוטומטית (אצל כולם — האחרון קובע). כדאי לנצל בקורס שרוצים לשפר."
- הסבר-נאדג' בהגדרות → "כשתפתחו את המלך והוא יראה משהו שדורש טיפול — הוא יגיד. הוא לא קופץ מעצמו."
- **ובנוסף: מעבר-קופי מלא** על he.json + מחרוזות inline בעברית: מסכי onboarding, planner, bidding, exam, settings, cohort. כלל: לקרוא בקול; אם רובוטי — לשכתב.

**#27 צ'יפים לא ממוגדרים ("מחליט/ה")**
- לאתר את כל הצעות-השאלות/צ'יפים עם /ה /ת → pg(gender). floating-assistant suggested questions + degree-qa.

**#28 עצת-בינארי חסרה עומק (הצטיינות, קורסים כבדים בראיונות)**
- להעשיר את תשובת-הבינארי (degree-qa + mentor context): (א) אזהרת-הצטיינות (25% מהשעות), (ב) "קורס-ליבה כבד (מיקרו/סטטיסטיקה) שמסתכלים עליו בהמשך — עדיף ציון מספרי גם אם בינוני", (ג) מותאם-מגדר.

### ד. מילואים — מקצה-שיפורים (הפער הגדול)

**#18+#32+#20+#21+#34**
- **עמוד `/miluim` ייעודי** (טאב בניווט, אייקון מגן):
  1. סטטוס: קבוצה לכל סמסטר (מ-MiluimSemester), ימים מצטברים, "נכון לסמסטר X".
  2. **הטבות אוטומטיות**: ש״ס-פטור מצטבר מחושב מהסמסטרים (C בשני סמסטרים ⇒ 8+8 מוגבל ל-10? לבדוק את החוק האמיתי ב-miluim.ts — לא להמציא!), בינארי-בשימוש נספר מהתוכנית (userCourse שסומן בינארי-מילואים) — לא קלט ידני. השדות הידניים בהגדרות הופכים ל"עקיפה ידנית" מוצנעת.
  3. העלאת 3010 (הרכיב המשותף מ-#2) + **תצוגת-תוצאה קריאה** (#21): טבלה — תקופה | ימים | סמסטר משויך | קבוצה; תקופות לא-משויכות בקבוצת "לפני התואר" מקופלת, לא דחוסות בשורה אחת.
  4. הסבר-הטבות המשוכתב (#22) + הערכה-חלופית (מתווה תשפ״ו) עם קישור להגדרה בתכנון-מבחנים (#34).
  5. הכפתור "עדכון נתוני שירות מילואים" בכל מקום → `/miluim` (#20).
- ההרשמה (#2) שואלת גם על סמסטרים קודמים דרך ה-3010 (התקופות משויכות אוטומטית).

**#34 הערכה-חלופית בתכנון-מבחנים**
- בקורסי המבחן ב-exam-planner: toggle לכל קורס "מבחן / עבודה / הערכה-חלופית" — עבודה/חלופית יוצאת מהסקייליין ומקבלת שורת-דדליין נפרדת. persist בהעדפות התוכנית.

### ה. בידינג — חשיבה-מחדש (#13+#15+#14+#12)
- **הבעיה**: הכלים יושבים סטטית ב-planner ורצים על קורסי-הסמסטר-הנוכחי (שכבר רשומים!) — חסר-ערך ומבלבל.
- **העיצוב-מחדש**:
  1. הבידינג עובר לעבוד על **הסמסטר הבא המתוכנן** (הקורסים המתוכננים שטרם נרשמו) — זה הסמסטר שמגישים עליו מכרז.
  2. כניסה עונתית: כרטיס בדשבורד לקראת פתיחת-סמסטר ("מתכננים את המכרז לסמסטר הבא? בדקו חפיפות") — לא תמידית.
  3. ב-planner: הבלוק מוצג רק כשיש סמסטר-עתידי מתוכנן, בכותרת "לקראת המכרז — הסמסטר הבא".
  4. סימולציית-הנקודות (worksheet): אם היא רק טבלת-עזר ידנית בלי נתונים אמיתיים — לצמצם ל"רשימת-בדיקה לפני הגשה" (חפיפות, עומס, גיבוי לקורס מבוקש) בלי מספרים. זכור: לעולם לא לנבא נקודות.
  5. הקופי השבור (#12/#14) משוכתב.
- אימות: משתמש-דמו רואה את הבלוק רק בהקשר הנכון; אפס אזכור לקורסים רשומים.

### ו. תכנון-מבחנים (#35+#36+#37+#39+#40+#42+#43)
- **#37**: מיון כרטיסי-הקורסים בתכנון לפי תאריך-מבחן עולה (היום — סדר קטלוגי).
- **#39**: "בנה לי תוכנית" disabled עד שהשלבים הושלמו (ימים, שעות, העדפות) עם tooltip "ענו קודם על…".
- **#35/36 ייצוא ותצוגה בתבנית-החברה**: גיליון-אקסל חדש ראשי: **לוח-שנה שבועי** — שורת-כותרת ימי-שבוע (ראשון←שבת RTL), בלוק לכל שבוע, כל תא: המקצוע של אותו יום (מהתוכנית), ימי-מבחן מודגשים "📝 מיקרו — מועד א׳", ימי-הוראה "סמסטר", צבע לכל קורס. פשוט, מודפס, כמו של החברה — רק מעוצב יותר. (הגיליונות הקיימים נשארים כלשוניות-משנה.)
- **שלב אינטראקטיבי לפני התוכנית**: וויזרד קצר (3 שאלות שכבר קיימות + חדשות: ימי-חופש, שעות-ביום מקס, אילו קורסים בהערכה-חלופית #34) → רק אז נבנית התוכנית.
- **#43 סקייליין ברור פי 10**: (א) tap על עמודה תמיד קופץ לאג'נדה (תוקן שהפסים בולעים tap — fall-through), (ב) מקרא צבעים קבוע, (ג) קו-היום בולט, (ד) תוויות ימים מודגשות בסופ"ש, (ה) בנייד — גלילה+tap בלבד (בלי drag).
- **#42 שיתוף וואטסאפ**: פורמט חדש: כותרת + אימוג'י-כדור לכל קורס + "📅 12.7 — מיקרו (מועד א׳)" שורה-לשורה ממוין, בלי בלגן, RTL נקי.

### ז. מבנה ושילוב
**#31 תיק-אקדמי מול מחשבון-ציונים**: לנתח ולבצע — record = "מה עשיתי" (עריכת ציונים, סריקה), graduation = "לאן אני הולך" (ממוצע-יעד, הצטיינות, סיום). למחוק כפילות-ממוצע-בכותרת, להוסיף קישורים-צולבים ("לעריכת ציונים → התיק"), ולתקן שמות-טאבים שמסבירים ("התיק האקדמי שלי · עבר" / "מחשבון ומסלול · עתיד"). לא ממזגים עמודים בסבב הזה.

**#24+#45 החברתי מלווה בכל מקום + בין-מחזורי**:
- קטלוג: בכרטיס-קורס שורת "חוכמת-מחזורים" (דירוג + טיפ אחד) כשיש נתונים (כבר קיים חלקית? לוודא חשיפה).
- planner: בבחירת קורס-בחירה — "מה אומרים עליו" מ-course-knowledge.
- exam-planner: טיפ-מחזור לקורס אם קיים.
- דשבורד: כרטיס-טיזר "תיק המחזור" עם התובנה הכי חדשה.
- **בין-מחזורי**: לוודא ש-course-knowledge מאגד על-פני כל המחזורים (לא רק שלו) — הידע עובר קדימה; בתצוגה לציין "מחזור תשפ״ה" ליד ביקורת.

**#19 אימוג'ים**: סריקה של כל האימוג'ים במוצר; החלפה לעקביים עם המותג (כתר/מגילה/עמודים). רשימת לפני/אחרי בדוח.

**#23 מדריך-מפתח-גוגל**: להחליף את הפסקה במיני-מדריך 4 צעדים ממוספרים עם צילומי-מסך מצוירים (SVG פשוטים inline: כפתור "Create API key" מודגש וכו'), שפה: "מפתח = כרטיס-כניסה אישי חינמי ל-AI של גוגל. לוקח דקה."

**#26 "פעולות בלתי-הפיכות"**: ניתוק-גוגל יוצא מאזור-הסכנה אל סקציית-היומן כפעולה רגילה ("ניתוק — אפשר לחבר שוב בכל רגע"). באזור נשארת רק מחיקת-חשבון.

**#38 דמות פיזית למלך**: איור SVG של מלך-פילוסוף (זקן, כתר-זהב, גלימה, מגילה) בסגנון שטוח-מודרני תואם-מותג: ב-FAB (ראש קטן), בפתיח-הפאנל, במסכי-ריק. אם הרפרנט פעיל — וריאציה. לבנות בקפידה; אם לא ברמה — עדיף הנוכחי (לא לשבור).

**#9 קופי-אנגלית "מתקדמים ב׳ · 1 קורסי רמה"**: לשכתב: "הרמה שלך: מתקדמים ב׳ — נשאר קורס-רמה אחד עד פטור. בנפרד, כל הפכ"מ נדרשים ל-2 קורסי-תוכן באנגלית". לתקן יחיד/רבים ("1 קורסי").

---

## אצוות-ביצוע (סדר)
1. **A-אמת**: #5 #4 #44 (סורק+אימות, סיכום-סריקה, מקור-ארזים)
2. **B-קופי+מגדר**: #3 #12 #14 #22 #25 #29 #33 #16 #27 #9 + מעבר מלא
3. **C-מלך**: #10 #30 #11 #28
4. **D-מילואים**: /miluim + #2 #18 #20 #21 #34
5. **E-בידינג**: #13 #15 (+קופי כבר ב-B)
6. **F-הרשמה/תכנון**: #1 #6 #7 #8 #17 #41
7. **G-מבחנים**: #35 #36 #37 #39 #40 #42 #43
8. **H-מבנה**: #31 #24 #45 #19 #23 #26 #38
- אחרי כל אצווה: tsc + vitest + בדיקת-דפדפן; deploy אחרי A, D, G, H (ובסוף הכול).
- דוח-סיום: סגירה הערה-הערה מול ההודעה המקורית + מה הוחלט לבד ולמה.
